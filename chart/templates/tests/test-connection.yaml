apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crossposter.fullname" . }}-test-connection"
  labels:
    {{- include "crossposter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crossposter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
